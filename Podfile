# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Practices (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Practices (iOS)
#pod 'Kingfisher', '~> 7.0'
pod 'GooglePlaces', '6.1.1'
pod 'Alamofire'
end

target 'Practices (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Practices (macOS)

end
